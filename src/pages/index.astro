---
/**
 * 首頁 (index.astro)
 * 
 * 用途：
 * - 顯示網站首頁
 * - 展示 Hero 區域和動畫效果
 * - 顯示最近更新的文章卡片
 * - 支持文章置頂功能
 * 
 * 功能：
 * - 從所有內容集合中獲取文章
 * - 按發布日期排序
 * - 分離置頂和普通文章
 * - 限制顯示數量（最多 12 篇）
 */

import BaseHead from "../components/BaseHead.astro";
import Footer from "../components/Footer.astro";
import Header from "../components/Header.astro";
import FormattedDate from "../components/FormattedDate.astro";
import { SITE_DESCRIPTION, SITE_TITLE } from "../consts";
import { getCollection } from "astro:content";
import "../styles/index.css";

// 圖片優化
import { getImage } from "astro:assets";
import myBackground from "../assets/hero_background.jpg";

/** 優化背景圖片為 WebP 格式以提高加載速度 */
const optimizedBackground = await getImage({
  src: myBackground,
  format: "webp",
});

// ============================================================================
// 內容獲取和處理
// ============================================================================

/** 從各個內容集合中獲取文章 */
const blogPosts = await getCollection("blog");
const devlogPosts = await getCollection("devlog");
const storyPosts = await getCollection("story");

/** 
 * 合併所有內容並添加類型標記
 * 這樣可以在模板中區分不同類型的文章
 */
const allPosts = [
  ...blogPosts.map(post => ({ ...post, type: "blog" as const, url: `/blog/${post.id}` })),
  ...devlogPosts.map(post => ({ ...post, type: "devlog" as const, url: `/devlog/${post.id}` })),
  ...storyPosts.map(post => ({ ...post, type: "story" as const, url: `/story/${post.id}` }))
];

/** 
 * 分離置頂文章
 * 置頂文章會顯示在最前面
 * 最多顯示 2 篇置頂文章
 */
const pinnedPosts = allPosts
  .filter(post => post.data.pinned === true)
  .sort((a, b) => new Date(b.data.pubDate).valueOf() - new Date(a.data.pubDate).valueOf())
  .slice(0, 2);

/** 
 * 普通文章
 * 按發布日期降序排列（最新的在前）
 * 數量 = 12 - 置頂文章數量
 */
const regularPosts = allPosts
  .filter(post => post.data.pinned !== true)
  .sort((a, b) => new Date(b.data.pubDate).valueOf() - new Date(a.data.pubDate).valueOf())
  .slice(0, 12 - pinnedPosts.length);

/** 最終顯示的文章列表（置頂在前，普通在後） */
const displayPosts = [...pinnedPosts, ...regularPosts];

/* ============================================================================
   Tag 統計與排序
   ============================================================================ */

/** 
 * 統計所有文章中每個 tag 的使用次數
 * 用於後續按使用頻率排序 tag
 */
const tagFrequency: Record<string, number> = {};
displayPosts.forEach(post => {
  if (post.data.tags) {
    post.data.tags.forEach(tag => {
      tagFrequency[tag] = (tagFrequency[tag] || 0) + 1;
    });
  }
});

/** 
 * 按使用次數排序 tag
 * 使用次數多的 tag 排在前面
 * 例如：['React', 'JavaScript', 'Web', 'CSS', ...]
 */
const sortedTags = Object.entries(tagFrequency)
  .sort((a, b) => (b[1] as number) - (a[1] as number))
  .map(entry => entry[0]);
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
  </head>
  <body>
    <Header />

    <main>
      <section
        class="hero"
        style={`background-image: url(${optimizedBackground.src});`}
      >
        <div class="hero-card">
          <div id="hero-btn">
            <span class="blob blob-1"></span>
            <span class="blob blob-2"></span>
            <span class="blob blob-3"></span>
            <span class="blob blob-4"></span>

            <div class="text-content">
              <h1 class="title">言の葉の栞へ</h1>
              <h1 class="title">ようこそ</h1>
            </div>
          </div>
        </div>
        <div class="hero-image">
          <!-- 這裡放一張圖片或簡單的色塊圖形 -->
          <div class="placeholder-img"></div>
        </div>
      </section>

      <div class="dummy-content" id="hero-content-start">
        <h2 class="section-title">最近更新</h2>
        <p class="section-subtitle">探索最新的文章、開發日誌和故事</p>
        
        {/* 主容器：卡片區域 + 側邊欄 */}
        <div class="content-wrapper">
          {/* 左側：文章卡片網格 */}
          <div class="cards-section">
            <div class="cards-grid">
          {displayPosts.map((post) => (
            <article class={`update-card ${post.data.pinned ? 'pinned-card' : ''}`}>
              {/* 卡片容器 - 不再是連結，只有按鈕是連結 */}
              <div class="card-link">
                {/* 內容區域 - 左側彈性寬度 */}
                <div class="card-main-content">
                  <div class="card-header">
                    <span class={`card-type card-type-${post.type}`}>
                      {post.type === 'blog' ? 'Blog' : 
                       post.type === 'devlog' ? 'DevLog' : 'Story'}
                    </span>
                    {post.data.pinned && (
                      <span class="pinned-badge">📌 置頂</span>
                    )}
                  </div>
                  
                  <div class="card-content">
                    <h3 class="card-title">{post.data.title}</h3>
                    <p class="card-description">
                      {post.type === 'story' 
                        ? `${'series' in post.data ? post.data.series : ''} - 第 ${'chapter' in post.data ? post.data.chapter : ''} 章`
                        : ('description' in post.data ? post.data.description : '暫無描述')
                      }
                    </p>
                    
                    {post.data.tags && post.data.tags.length > 0 && (
                      <div class="card-tags">
                        {/* 
                          Tag 顯示邏輯：
                          1. 按全站 tag 使用頻率排序（使用次數多的排在前）
                          2. 只顯示前 3 個最常用的 tag
                          3. 如果文章少於 3 個 tag，就全部顯示
                        */}
                        {post.data.tags
                          .sort((a, b) => sortedTags.indexOf(a) - sortedTags.indexOf(b))
                          .slice(0, 3)
                          .map((tag) => (
                            <span class="tag">{tag}</span>
                          ))}
                      </div>
                    )}
                  </div>
                  
                  <div class="card-meta">
                    {/* 日期 - 灰色標籤樣式 */}
                    <span class="card-date">
                      <FormattedDate date={post.data.pubDate} />
                    </span>
                    {/* 閱讀更多按鈕 - 唯一的可點擊元素 */}
                    <a href={post.url} class="read-more">閱讀更多 →</a>
                  </div>
                </div>
                
                {/* 圖片區域 - 右側動態寬度 */}
                {(() => {
                  const thumbnail = 'thumbnail' in post.data ? post.data.thumbnail : undefined;
                  const coverImage = 'coverImage' in post.data ? post.data.coverImage : undefined;
                  const imageUrl = thumbnail || coverImage;
                  
                  /* 如果有圖片就顯示，沒有就不顯示任何東西 */
                  return imageUrl ? (
                    <div class="card-image">
                      <img src={imageUrl} alt={post.data.title} />
                    </div>
                  ) : null;
                })()}
              </div>
            </article>
          ))}
            </div>
          </div>

          {/* 右側：個人資訊側邊欄 */}
          {/* 
            Sticky Sidebar：固定在視窗內滾動
            
            說明：
            - position: sticky 讓側邊欄隨著 content-wrapper 滾動
            - JavaScript 會動態計算 top 值，防止進入 Hero 或 Footer
            - 側邊欄內容不滾動，整體隨著卡片滾動
          */}
          <div class="sidebar" id="sticky-sidebar">
            {/* 
              側邊欄內容容器
              
              說明：
              - 包裹所有側邊欄內容（頭像、名稱、簡介、連結）
              - 當內容超過側邊欄高度時，此容器會顯示滾軸
              - 允許側邊欄內容獨立滾動，而側邊欄本身保持 sticky 定位
            */}
            <div class="sidebar-content">
              {/* 頭像 */}
              <div class="sidebar-avatar">
                <img src="https://hackmd.io/_uploads/Bk3Z1Uzm-e.png" alt="個人頭像" />
              </div>

              {/* 名稱 */}
              <a href="/about" style = "text-decoration: none;">
                <h3 class="sidebar-name">言葉</h3>
              </a>
              {/* 簡介 */}
              <p class="sidebar-bio">
                我是言葉，一個不專業的小說家兼繪師兼碼農。<br><br>
                是個一是處的天才。大概吧...
              </p>

              {/* 社群連結 */}
              <div class="sidebar-links">
                <a href="https://github.com/DanDingTangYuan" target="_blank" rel="noopener noreferrer" title="GitHub">
                  <svg viewBox="0 0 24 24" fill="currentColor">
                    <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v 3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
                  </svg>
                </a>
                <a href="https://x.com/kaichou_73" target="_blank" rel="noopener noreferrer" title="Twitter">
                  <svg viewBox="0 0 24 24" fill="currentColor">
                    <path d="M23 3a10.9 10.9 0 01-3.14 1.53 4.48 4.48 0 00-7.86 3v1A10.66 10.66 0 013 4s-4 9 5 13a11.64 11.64 0 01-7 2s9 5 20 5a9.5 9.5 0 00-9-5.5c4.75 2.25 7-7 7-7"/>
                  </svg>
                </a>
                <a href="mailto:your.email@example.com" title="Email">
                  <svg viewBox="0 0 24 24" fill="currentColor">
                    <path d="M24 5.457v13.909c0 .904-.732 1.636-1.636 1.636h-3.819V11.73L12 16.64l-6.545-4.91v9.273H1.636A1.636 1.636 0 0 1 0 19.366V5.457c0-2.023 2.309-3.178 3.927-1.964L5.455 4.64 12 9.548l6.545-4.91 1.528-1.145C21.69 2.28 24 3.434 24 5.457z"/>
                    <polyline points="22,6 12,13 2,6" fill="none" stroke="currentColor" stroke-width="2"/>
                  </svg>
                </a>
              </div>
            </div>
          </div>
        </div>
        
        <div class="view-all-links">
          <a href="/blog" class="view-all-btn">查看所有 Blog</a>
          <a href="/devlog" class="view-all-btn">查看所有 DevLog</a>
          <a href="/story" class="view-all-btn">查看所有 Story</a>
        </div>
      </div>
    </main>
    <Footer />
  </body>
</html>

<script>
  /**
   * 頁面滾動事件處理
   * 
   * 功能：
   * 1. Header 顯示/隱藏和顏色變化
   * 2. Hero 卡片點擊事件
   */

  // ============================================================================
  // 元素選擇
  // ============================================================================

  const header = document.querySelector("header");
  const hero = document.querySelector(".hero");
  const heroCard = document.querySelector(".hero-card");

  // ============================================================================
  // Header 滾動控制
  // ============================================================================

  let lastScrollY = window.scrollY;

  if (header && hero) {
    window.addEventListener("scroll", () => {
      const currentScrollY = window.scrollY;
      // @ts-ignore
      const heroHeight = hero.offsetHeight;

      // --- Header 顯示/隱藏 (依據滾動方向) ---
      if (currentScrollY > lastScrollY && currentScrollY > 50) {
        // 向下滾 -> 隱藏
        header.classList.add("nav-hidden");
      } else {
        // 向上滾 -> 顯示
        header.classList.remove("nav-hidden");
      }

      // --- Header 顏色/透明度 (依據位置) ---
      if (currentScrollY > heroHeight - 80) {
        // 離開 Hero -> 變成實色
        header.classList.add("nav-solid");
      } else {
        // 還在 Hero 內 -> 移除實色
        header.classList.remove("nav-solid");
      }

      // 更新最後滾動位置
      lastScrollY = currentScrollY;
    });
  }

  // ============================================================================
  // Hero 卡片點擊事件 - 平滑滾動到內容區域
  // ============================================================================

  if (heroCard) {
    heroCard.addEventListener("click", () => {
      const targetElement = document.querySelector("#hero-content-start");
      if (targetElement) {
        targetElement.scrollIntoView({ behavior: "smooth", block: "start" });
      }
    });
  }

  // ============================================================================
  // Sticky Sidebar 滾動控制
  // ============================================================================

  /**
   * Sticky Sidebar 效果
   * 
   * 功能：
   * 1. 側邊欄根據滾動位置動態調整 top 值
   * 2. 防止側邊欄進入 Hero 區塊（頂部限制）
   * 3. 防止側邊欄進入 Footer 區塊（底部限制）
   * 
   * 邏輯說明：
   * - 當滾動位置在 Hero 區塊時，側邊欄卡在 dummy-content 的頂部邊界
   * - 當滾動進入 dummy-content 時，側邊欄固定在視窗頂部 100px 處
   * - 當滾動接近 Footer 時，側邊欄卡在 Footer 的頂部邊界
   */

</script>

<style>
  /* 為了讓滑動停下來的位置剛好避開 Header (如果 header 會擋住標題的話) */
  #content-start {
    scroll-margin-top: 80px; /* 假設你的 Header 高度是 80px */
  }

  #content-start h1 {
    font-size: 2.5rem;
    margin: 0 0 0.5rem 0;
    color: #2c3e50;
  }
</style>
